apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-config
  namespace: microservices
data:
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "auth_db"
  JWT_ISSUER: "cec-auth-service"
  JWT_EXPIRATION_HOURS: "24"